# 拆一条：V0.1 产品与交互设计

## 1. 设计依据

设计目标：减少个人创作者手动拆解短视频时的重复整理工作。文档版本：2026-09-15。

用户的实际痛点是反复观看、暂停、抄写和对齐画面与文案。目标是得到一份能直接进入后续拍摄准备的参考资料。

产品定位：**把一条参考视频变成可校对、可复用的分镜脚本表。**

第一版目标人群：学习短视频表达、需要反复拆解中文口播或简单演示视频的个人创作者。优先测试 30–90 秒的视频；该范围是验证范围，不是模型能力承诺。

## 2. 版本范围

| 环节 | 本次交互原型 | 正式 AI MVP |
| --- | --- | --- |
| 视频输入 | 本地文件选择、拖放、播放、真实元数据 | 增加服务端任务与上传进度 |
| 镜头划分 | 示例序列、手动新增、编辑起止时间 | 根据画面变化检测边界 |
| 代表截图 | 示例插画；真实视频在新增镜头时截帧 | 按镜头生成代表帧 |
| 文案 | 示例口播与屏幕文字、手动编辑 | 分别接入语音转写与文字提取 |
| 画面描述 | 示例与手动记录 | 视频或图像理解返回初稿 |
| 内容作用与结构 | 独立标记为分析判断，示例可编辑 | 与事实提取分开生成和保存 |
| 校对 | 时间跳转、待核实筛选、进度、保存 | 保留模型初稿与用户修订版本 |
| 导出 | CSV、Markdown、TXT、制表符复制 | 可增加嵌入截图的 XLSX |

暂不纳入：平台链接下载、批量分析、自动剪辑、账号系统、爆款评分、直接生成我的成片或改编脚本。

## 3. 核心流程

目标流程：选择视频 → AI 生成初稿 → 对照原片校对 → 导出。

原型体验有两个清楚的入口：

1. **示例项目**：直接进入完整结果页，体验目标输出和校对交互。页面和导出文件均带演示标识。
2. **本地视频**：真实读取视频，只创建手动草稿。界面明确显示 AI 尚未接入，允许手动添加分镜。

不以等待动画或随机生成文字伪装视频分析。

## 4. 信息架构

### 工作台

- 左侧导航：新建拆解、当前项目、示例入口、本地草稿恢复、使用指南。
- 页面头部：项目名、项目类型、上传与导出入口。
- 流程条：提示用户正在校对阶段。
- 左栏原片：视频播放器、文件信息、时长、分镜数、待核实数、校对进度。
- 右栏结果：分镜脚本 / 完整口播 / 内容结构。

### 分镜脚本

主表优先展示需要频繁核对的内容：

| 信息 | 呈现和交互 |
| --- | --- |
| 编号、代表画面 | 缩略图，点击播放对应时间 |
| 起止时间 | 点击定位；约值显示 `≈` |
| 画面与动作 | 表格内直接编辑 |
| 拍摄信息 | 简短标签，点击进入详情编辑 |
| 口播原文 | 独立列，保留原话，不默认润色 |
| 屏幕文字 | 独立列，区别于口播 |
| 待核实 | 琥珀色标识；详情窗口显示具体问题 |
| 校对状态 | 圆形勾选按钮，驱动进度统计 |
| 内容作用 | 放在详情窗口的独立分析区域，随导出保留 |

### 完整口播

按时间排列的连续阅读视图。与主表共享同一份口播数据，避免两处修改不同步。可复制全文或导出纯文本。

### 内容结构

独立保存观点和叙事分段。示例中 6 个镜头对应 4 个内容阶段，直观表达“镜头切换不等于观点变化”。此区域有明确的分析判断标识。

## 5. 关键交互规则

1. 时间使用秒数存储，显示为 `mm:ss`，需要时精确到十分之一秒。
2. `0 ≤ start < end ≤ duration`，同一视频的分镜不得重叠。
3. 本地手动记录优先向后追加；已有分镜覆盖整条视频时，新增操作拆分当前镜头。保存前不修改原镜头。
4. 点击镜头播放时间点，播放器继续播放原片，随当前时间更新选中分镜；当前版不循环单段。
5. 改动表格中的事实字段后取消该分镜的“已校对”状态。
6. 听不清、看不清、无法判断的内容可留空或写“待核实”，不自动补写。
7. “待核实”统计指带有疑问且未标记校对完成的分镜数；已核实后的历史疑问仍随导出保留。
8. 编辑 250 毫秒后保存草稿；存储失败时提示及时导出。
9. 导出保留数据来源、时间约值、核实说明及校对状态。
10. 桌面采用双栏对照，窄屏将原片与概览放到结果上方，表格允许横向浏览。

## 6. 视觉方向

名称“拆一条”强调一次只完成一条视频的拆解。米白背景、深灰绿正文、浅芽绿色主按钮，营造安静的创作工作台。

- 绿色用于选中和校对完成。
- 琥珀色用于待核实，不把低置信结果包装成确定事实。
- 淡紫色用于分析判断，与视频原始内容区分。
- 主表使用真实可编辑文字，不把结果画成图片。
- 原创桌面整理插画仅承担演示素材作用，不暗示真实转写效果。

## 7. 正式 AI MVP 的接入设计

以下是下一阶段建议，尚未实现，也未将分享中的仓库效果视为实测结论。

```text
前端创建任务
    → 服务端接收单条视频
    → 获取时长 / 音轨 / 代表帧
    → 镜头边界检测 + 语音转写
    → 按时间对齐画面、口播和屏幕文字
    → 生成单独的内容作用及结构分析
    → 校验结构化字段和时间范围
    → 返回可校对初稿
```

后台任务建议提供 `queued / processing / completed / failed / cancelled` 状态；处理中显示当前阶段，不在缺少真实进度时编造百分比。失败保留文件和已完成数据，提供重试入口。模型密钥只放在服务端。

### 数据模型

```ts
type Shot = {
  id: string;
  start: number;
  end: number;
  approximate: boolean;
  image?: string;
  visual: string;
  camera: string;
  speech: string;
  screen: string;
  analysis?: string;     // 内容作用，明确属于判断
  uncertainty: string;
  reviewed: boolean;
};

type ContentSection = {
  id: string;
  start: number;
  end: number;
  title: string;
  role: string;
  analysis: string;
};

// 正式版应另存 initialModelOutput、userRevision、
// transcription segments、field-level confidence 和原视频摘要。
```

分享对话中提到的 BiliNote、PySceneDetect、faster-whisper、VideoCaptioner、Gemini Cookbook 可作为下一阶段的流程或组件候选。当前没有复制其代码、安装其服务或验证相关仓库效果。

## 8. 验收设计

### 交互验收

- 选择合法视频后能真实播放，显示正确时长。
- 表格文字修改后在完整口播视图同步。
- 点击镜头时间能定位原片；播放时突出当前分镜。
- 待核实筛选正确，标记完成后计数与进度同步变化。
- 无效时间和重叠时间不能保存。
- 刷新后草稿仍在；缺少原片时清楚提示重新选择。
- 示例与本地草稿可以切换，恢复后文字仍在。
- 导出包含校对后文本、原文与分析的区别、待核实项和来源标识。
- 窄屏页面无整体横向溢出，长表格在自身区域横向滚动。

### 真实 AI 验收（待接入后）

选择 3–5 条有权使用的视频，至少一条建立人工拆解表。记录手工拆解和工具辅助的完整耗时，包括上传、等待、校对和导出。

比较关键文案遗漏、原文改写、镜头错分、时间定位偏差、无依据补写以及人工修改量。是否减少总耗时、拆解表能否用于准备下一条视频，是主判断标准。

“耗时减少一半”只能作为试验目标，在实测前不作为宣传文案。
