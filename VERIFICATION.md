# V0.2 本地基础版验证记录

日期：2026-09-15。平台：当前 Apple Silicon Mac，Python 3.12，浏览器本地工作台。

## 自动化检查

`tests/test_local.py` 的 5 项测试通过：

- 自制 3 秒、三色硬切视频：检测为 0–1、1–2、2–3 秒三个分镜，均生成内嵌 JPEG；没有音轨时口播留空。
- 跨镜头词级对齐：每个词只出现一次，按最大重叠时间分配。
- 取消处理、视频时长上限拦截。
- HTTP 上传 → 后台处理 → 读取结果 → 保存修改 → 磁盘读回；视频范围请求返回正确字节；重叠分镜保存被拒绝。
- 拒绝外部 Host/Origin、无令牌写入、私有目录和模型路径访问。

JavaScript 语法、Python 编译、启动脚本语法和 `git diff --check` 通过。

## 真实本地语音推理

通过 `scripts/make_local_test.py` 用 macOS 系统中文语音生成 12.59 秒测试口播，并编码为含三个画面的 MP4；不是用户私人音视频。

- 使用已下载的 Whisper base 多语言模型、CPU INT8、词级时间戳和 VAD。
- 在 Python socket 连接被测试桩禁止的条件下，完成视频解码、截图和语音推理，未触发网络连接。
- 视频时长读为 12.6 秒，分镜边界为约 4.28、8.40 秒，三个分镜均含截图与口播。
- 单次本机引擎测试约 4.46 秒；只反映此合成小样本，不代表一般处理速度。
- 测试口播中的“整洁”曾识别为“整结”，证明需要人工校对；已通过界面修改并验证保存。

## 浏览器验证

- 实际通过文件选择器导入 MP4，点击开始，观察阶段进度，完成后展示三个自动分镜。
- 修改镜头 3 的口播，在完整口播页显示相同修改；从另一来源地址的项目库重新打开后，磁盘中的修改仍在。
- 刷新自动恢复原片，无需重新选择文件。
- HTML 报告真实下载到本机：三个内嵌 JPEG、三个分镜、校对后的文字、无脚本。
- SRT 真实下载：三个字幕块、毫秒时间、校对后的口播。
- CSV 真实下载：三个数据行、十二个字段、保留本地模型来源标识。
- 检查了桌面工作台、导入选项、处理窗口、项目库和导出窗口；检查时未记录到 JavaScript 错误。

## 验证边界

未做真实视频集的识别准确率评测；未验证所有编码、渐变转场、复杂运动、多音轨选择、长视频压力及 Windows/Intel Mac。画面语义描述、OCR 和结构总结不在当前实现内。自动分镜与转写均需校对。

---

## 旧版历史记录

# V0.1 验证记录

日期：2026-09-15。

通过 Codex 内置浏览器操作真实页面验证；上传测试使用本项目自制的演示视频，没有调用第三方 AI 服务。

| 检查 | 结果 |
| --- | --- |
| 示例资源播放 | 真实 MP4，浏览器读取时长 36 秒，视频 readyState 为 4 |
| 时间定位 | 点击镜头 4 后播放器位于约 16.18 秒，对应行 `s4` 选中 |
| 表格与完整口播同步 | 修改镜头 1 口播后，完整口播视图出现相同文本 |
| 待核实筛选 | 初始 6 行筛选为 2 行；校对镜头 3 后筛选结果降为 1 行，进度更新为 1/6 |
| 时间边界 | 将镜头 2 的结束时间设为 50 秒时，保存被拒绝，提示最大时长 36 秒 |
| 本地文件输入 | 文件选择器载入项目内的 `demo.mp4`，真实读取 36 秒并显示本地手动草稿 |
| 手动追加分镜 | 首个分镜 0–4 秒保存后，下一次添加默认范围为 4–8 秒 |
| 真实代表画面 | 本地新增镜头后生成视频截帧缩略图 |
| 刷新与恢复 | 手动口播文本保留，提示重新选择原片 |
| 示例和本地草稿切换 | 回到本地草稿后保留已输入口播 |
| 分镜表复制 | 从浏览器剪贴板读回包含 6 个分镜的制表符文本 |
| CSV 下载 | 从下载目录读取实际文件，验证 6 个数据行、12 个字段，保留来源和待核实信息 |
| Markdown 下载 | 实际文件包含分镜表、完整口播和独立内容结构区域 |
| TXT 下载 | 实际文件保留时间、待核实和无音频示例说明 |
| 分镜分析字段 | 详情窗口显示单独的“内容作用（分析判断，非原片事实）”字段 |
| 内容结构 | 与 6 个分镜独立的 4 个内容阶段 |
| 窄屏布局 | 390 像素窗口（滚动条后内容宽 375）；页面宽与可视内容宽一致，620 像素表格只在自身区域滚动 |
| 视觉检查 | 检查桌面工作台、内容结构、上传弹窗及窄屏布局 |
| 浏览器控制台 | 验证时未记录到页面 JavaScript 错误 |
| 静态检查 | `node --check app.js`、`node --check server.mjs`、`git diff --check` 通过 |

未验证也未实现：真实 AI 识别准确率、转写质量、自动镜头检测、服务端任务处理、跨浏览器兼容、真实效率提升。
